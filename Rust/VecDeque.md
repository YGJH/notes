```rust
let mut dp = VecDeque::new().with_capacity(VecDeque::new().with_capacity(i32mn , n+1) , n+1);
```

```rust
let mut dp = VecDeque::with_capacity(n + 1);
```

```rust
let mut dp = vec![vec![neg_inf; x + 1]; n + 1];
```
